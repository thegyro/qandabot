from __future__ import unicode_literals

from django.db import models
from django.contrib.auth.models import User

# # Create your models here.
# class Conversation(models.Model):
# 	conversation_id = models.
# 	message = models.models.TextField(blank=False,null=False, default='0')
#     conversation_timestamp = models.DateTimeField('Sent at:', auto_now_add=True)

# class Account(models.Model):
#     user = models.OneToOneField(User)
#     conversation_id = models.ForeignKey(Conversation,null=False)